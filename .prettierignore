CHANGELOG.md
/ecosystem-tests/*/**
/node_modules
/deno

# don't format tsc output, will break source maps
dist

# Stainless emits this with non-prettier-compliant array expansion; auto-formatting trips CI.
packages/mcp-server/manifest.json
